# Gluten vs Native Bench 报告 — 101.43.161.139（cosn 数据 / 80GB 5 表）

> 📌 **本报告为模板骨架**，跑批结束后由 `run_bench_101.sh` 把以下占位符回填：
> - `<TS>`：实际跑批的 `YYYYMMDD_HHMMSS`
> - `<DEFAULT_JDK>`：来自 `inspect_101.sh` 输出的「默认 JDK 版本」
> - `<JDK17_HOME>`：bench 脚本运行期探测到的 JDK17 路径（若无则填 `不存在`）
> - 7 条 SQL 的耗时/加速比/一致性：直接从 `summary_<TS>.md` 复制
> - 数据盘点表：直接从 `data_inventory.txt` 复制

---

## 0. 红线声明

> ✅ **本次测试未修改任何集群基础环境**：
> - **集群默认 JDK 始终保持 JDK8**：未卸载、未替换 `/usr/bin/java`、未改 `/etc/profile`、未动 `update-alternatives`；
> - **未修改任何 `*-site.xml`**（core-site / hdfs-site / yarn-site / hive-site / spark-defaults 等）；
> - 仅在用户态 `/root/bench-101/` 写入测试资产；
> - JDK17 切换 **仅通过 Spark 运行期 `--conf`** 实现，作业结束即失效。

### 0.1 本次仅做的运行期参数清单（`--conf`）

通用：
- `spark.master=yarn` / `spark.submit.deployMode=client`
- `spark.eventLog.enabled=false`
- `spark.sql.adaptive.enabled=false`
- `spark.sql.shuffle.partitions=50`
- `spark.sql.hive.convertMetastoreParquet=true`
- `spark.sql.parquet.enableVectorizedReader=true`
- `--database bench_cosn`

Native 特有：无附加参数（直接走集群默认 JDK8）。

Gluten 特有（仅在检测到 JDK17 时启用）：
- `spark.yarn.appMasterEnv.JAVA_HOME=<JDK17_HOME>`
- `spark.executorEnv.JAVA_HOME=<JDK17_HOME>`
- `spark.driver.extraJavaOptions=<JDK17 add-opens 串>`
- `spark.executor.extraJavaOptions=<JDK17 add-opens 串>`
- `spark.plugins=org.apache.gluten.GlutenPlugin`
- `spark.memory.offHeap.enabled=true` / `spark.memory.offHeap.size=3g(light) | 8g(standard)`
- `spark.shuffle.manager=org.apache.spark.shuffle.sort.ColumnarShuffleManager`
- `spark.gluten.memory.allocator=jemalloc`
- `spark.gluten.sql.columnar.backend.velox.partialAggregationMemoryRatio=0.5`
- `spark.gluten.sql.columnar.backend.velox.spillEnabled=false`（避开 sumVector 坑位）

---

## 1. 环境盘点表

> 数据来源：`reports/101.43.161.139_<TS>_inspect.md`

| 项 | 值 |
|---|---|
| 主机 | 101.43.161.139 |
| 默认 JDK（基础环境，未改） | `<DEFAULT_JDK>` |
| `readlink -f /usr/bin/java` | `<待回填>` |
| 其他 JDK 路径 | `<待回填>` |
| Spark 版本 / 路径 | `<待回填>` |
| Hadoop 版本 / `HADOOP_CONF_DIR` | `<待回填>` |
| Hive 版本 | `<待回填>` |
| YARN 队列 | `<待回填>` |
| cosn 插件 jar | `<待回填，若缺失则标注「缺失，已停止 cosn 测试」>` |
| Gluten / Velox jar | `<待回填，若缺失则标注「缺失，已仅推进 Native 部分」>` |

---

## 2. 数据盘点表

> 数据来源：`/root/bench-101/data_inventory.txt`

| 表 | cosn 路径 | 大小 | 抽检 count(*) | 状态 |
|---|---|---|---|---|
| customers   | `cosn://lkl-bj-update-1308597516/meson/data/bench/customers`   | `<待回填>` | `<待回填>` | OK / MISSING |
| products    | `cosn://lkl-bj-update-1308597516/meson/data/bench/products`    | `<待回填>` | `<待回填>` | OK / MISSING |
| reviews     | `cosn://lkl-bj-update-1308597516/meson/data/bench/reviews`     | `<待回填>` | `<待回填>` | OK / MISSING |
| orders      | `cosn://lkl-bj-update-1308597516/meson/data/bench/orders`      | `<待回填>` | `<待回填>` | OK / MISSING |
| order_items | `cosn://lkl-bj-update-1308597516/meson/data/bench/order_items` | `<待回填>` | `<待回填>` | OK / MISSING |

---

## 3. 运行参数表

| 维度 | light（默认） | standard |
|---|---|---|
| `--num-executors` | 6 | 9 |
| `--executor-cores` | 2 | 5 |
| `--executor-memory` | 2g | 5g |
| `--driver-memory` | 2g | 4g |
| Gluten offHeap.size | 3g | 8g |
| 适用场景 | 资源受限 / 冒烟 | 性能基线对齐 `bench-toolkit` 默认 |

本次实际使用：`<待回填，light 或 standard>`
JDK17 路径（运行期切换用）：`<JDK17_HOME>`

---

## 4. 7 条 SQL 耗时对比表

> 数据来源：`results/<TS>/summary_<TS>.md`（直接整表复制即可）

| SQL | Native(s) | Gluten(s) | Speedup | Consistent | 备注 |
|---|---|---|---|---|---|
| q1_agg | `<>` | `<>` | `<>` | `<>` | `<>` |
| q2_join_2tables | `<>` | `<>` | `<>` | `<>` | `<>` |
| q3_join_3tables | `<>` | `<>` | `<>` | `<>` | `<>` |
| q4_window | `<>` | `<>` | `<>` | `<>` | `<>` |
| q5_subquery | `<>` | `<>` | `<>` | `<>` | `<>` |
| q6_self_join | `<>` | `<>` | `<>` | `<>` | `<>` |
| q7_5tables | `<>` | `<>` | `<>` | `<>` | `<>` |

**总体加速比（几何平均）**：`<待回填>`

---

## 5. 失败 / 已知坑位记录

> 数据来源：`results/<TS>/*.diag` + summary 表中的 `[已知坑位]` 标记

| SQL | mode | applicationId | 失败原因（首条 ERROR） | 已知坑位 | 建议解法 |
|---|---|---|---|---|---|
| `<>` | native/gluten | `<>` | `<>` | ETypeConverter / sumVector / ByteBuffer.flip / 无 | `<>` |

> **三大已知坑位与建议解法（仅建议，不自动改环境）**：
> 1. **ETypeConverter** → 增加 `--conf spark.sql.hive.convertMetastoreParquet=true`（脚本已默认带）
> 2. **VeloxUserError: sumVector != nullptr** → 关 Velox spill `--conf spark.gluten.sql.columnar.backend.velox.spillEnabled=false`（脚本已默认带）
> 3. **NoSuchMethodError: ByteBuffer.flip()** → 装/已有 Kona JDK11/17 + 透传 `spark.executorEnv.JAVA_HOME`（脚本已默认带，前提是机器上有 JDK17 路径）

---

## 6. 结论与下一步建议

### 6.1 结论
- `<待回填：例如 在 light 档位、cosn 5 表数据上，7 条 SQL 中 X 条成功、平均加速比 X.XX×>`
- `<待回填：是否所有 SQL 一致性 YES>`
- `<待回填：cosn 上 Gluten 加速比与 HDFS 上的预期值（1.2-1.3×）的对比>`

### 6.2 下一步建议
- 若 light 档位通过：考虑切到 `standard` 档位再跑一轮以贴近 `bench-toolkit` 性能基线
- 若机器上无 JDK17：建议运维另行预装一个 JDK17（**安装到独立目录如 `/opt/jdk-17/`，不替换默认 JDK**），下次跑批用 `JDK17_HOME_OVERRIDE=/opt/jdk-17 bash bench_yarn_cosn.sh ...`
- 若 cosn 第一条就失败：联系运维确认 `core-site.xml` 中是否配置了 `fs.cosn.impl` / `fs.cosn.userinfo.secretId` / `fs.cosn.userinfo.secretKey`（仅请求确认，不擅自修改）

---

## 7. 未达成项（若有）

| 需求项 | 未达成原因 | 影响面 |
|---|---|---|
| `<待回填或写「无」>` | `<>` | `<>` |

---

## 附录 A：本次执行的资产清单

| 文件 | 用途 |
|---|---|
| [inspect_101.sh](../20260525-bench/inspect_101.sh) | 集群环境只读盘点 |
| [deploy_to_101.sh](../20260525-bench/deploy_to_101.sh) | 资产部署到 `/root/bench-101/` |
| [external_tables_meson.sql](../20260525-bench/external_tables_meson.sql) | cosn 外部表 DDL（库 `bench_cosn`） |
| [prepare_cosn_tables.sh](../20260525-bench/prepare_cosn_tables.sh) | 远端数据探查 + 建表 |
| [bench_yarn_cosn.sh](../20260525-bench/bench_yarn_cosn.sh) | Native vs Gluten 双跑 + summary |
| [run_bench_101.sh](../20260525-bench/run_bench_101.sh) | 一键编排（盘点 → 部署 → 建表 → 跑批 → 回收） |

## 附录 B：复跑步骤

```bash
# 在本地执行（前提：能 ssh -i lkl_meson.pem root@101.43.161.139）
cd D:/bigdata/gitproject/bigdata-study/spark/案例/向量化Gluten/20260525-bench

# 一键全流程（默认 light 档位）
bash run_bench_101.sh

# 标准档位
bash run_bench_101.sh standard

# 单独某一阶段：
bash inspect_101.sh                               # 仅盘点
bash deploy_to_101.sh                             # 仅部署
ssh -i ~/lkl_meson.pem root@101.43.161.139 \
  "bash /root/bench-101/prepare_cosn_tables.sh"   # 仅建表
ssh -i ~/lkl_meson.pem root@101.43.161.139 \
  "bash /root/bench-101/bench_yarn_cosn.sh /root/bench-101/queries_cosn light"  # 仅跑批
```
